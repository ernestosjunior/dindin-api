## Dindin-api

### Instructions to run project:

1. Run `yarn` command to install dependences
2. Create file `.env` and add at enviroments variables in `.env.example`
3. Run `yarn dev` command to run in development mode 
4. Run `yarn start` command to run in production mode
